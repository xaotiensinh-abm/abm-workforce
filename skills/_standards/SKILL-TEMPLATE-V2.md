---
name: {skill-name}
description: >
  {Mô tả chi tiết, "pushy", cover nhiều cách user có thể hỏi.
  Include: what it does, when to use, trigger keywords.
  Tối thiểu 2 dòng, tối đa 5 dòng.}
metadata:
  version: "1.0"
  author: ABM-Workforce
  worker-id: {W1-W10}         # Worker chịu trách nhiệm chính
  maturity: "Tier-B"          # S / A / B / C
  complexity: 0               # Score 1-25+ (từ Skill Creator Phase 3)
  category: development       # development / business / content / design / ops / security / utility
  tags: []                    # Semantic tags cho search & routing
  depends-on: []              # Skill dependencies
  last-updated: YYYY-MM-DD
  trigger-accuracy: null      # 0.0-1.0 (từ Phase 8 evaluation)
---

# Goal

{Mục tiêu duy nhất của skill. 1-3 câu. Trả lời: "Skill này tồn tại để làm gì?"}

# Instructions

{Quy trình step-by-step. Mỗi bước rõ ràng, actionable.
Nếu skill phức tạp (complexity > 12), tách logic chi tiết sang references/:
  - "Đọc `references/domain-knowledge.md` khi cần X"
  - "Đọc `references/schemas.md` khi cần Y"
}

## Tài nguyên tham chiếu (nếu có)
{Liệt kê references/ files và khi nào cần đọc}

# Examples

## ✅ Ví dụ 1: Happy Path — Trường hợp chuẩn
**Input**: {Input cụ thể}
**Output**: {Output cụ thể, hoàn chỉnh}

## ⚠️ Ví dụ 2: Edge Case — Trường hợp biên
**Input**: {Input khó}
**Output**: {Output xử lý đúng}

## ❌ Ví dụ 3: Anti-Example — KHÔNG nên làm thế này
**Input**: {Input}
**Wrong output**: {Output sai}
**Why wrong**: {Giải thích tại sao sai}
**Correct output**: {Output đúng}

# Output Contract

## Required Sections
{Checklist các phần bắt buộc trong output}

## Format
{Quy định format: markdown, YAML, JSON, code...}

## Self-Check Rubric (0-10)
{3-5 tiêu chí tự chấm trước khi trả output cho user}

# Constraints

{Giới hạn, ranh giới, và điều KHÔNG BAO GIỜ làm.
Giải thích WHY cho mỗi constraint — AI hiểu lý do → follow tốt hơn.}

<!-- 
  9-Layer Compliance:
  L0: Use Case & Trigger Map  → description + metadata.tags
  L1: Metadata                → frontmatter YAML  
  L2: Core SKILL.md           → Goal + Instructions + Examples + Constraints
  L3: References              → references/ folder (nếu complexity > 12)
  L4: Examples                → 3 categories: happy, edge, anti
  L5: Scripts & Tools         → scripts/ folder (nếu complexity > 20)
  L6: Assets & Templates      → assets/templates/ folder
  L7: Output Contract & QC    → Output Contract section
  L8: Governance & Evolution   → metadata.version + CHANGELOG.md
-->
<!-- Generated by ABM-Workforce Skill Template v2.0 — 9-Layer Compliant -->
